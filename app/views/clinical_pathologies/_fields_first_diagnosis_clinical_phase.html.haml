- required = "必填"
.nested-fields
  %fieldset
    %legend
      初诊临床分期
      = link_to_remove_association "delete", f, data: {confirm: "Do you wanna to delete?"}

    - if f.object.persisted?
      = f.hidden_field :id, value: f.object.id

    .row
      = f.input :primary_lesion, front_error: required
      = f.input :regional_lympth_node, front_error: required
      = f.input :matastasis, front_error: required