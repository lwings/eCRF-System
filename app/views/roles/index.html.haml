- content_for :side_bar do
  = render 'shared/sidebar_project'

%h2
  角色列表
  %small 共有#{@roles.total_count}个

- operations = {view:{ }}

= render partial: 'shared/table', locals: { columns: [:name,:created_at], collection: @roles, operations: operations,
                                        url_method: {view: (method :role_path)} }

= paginate @roles

